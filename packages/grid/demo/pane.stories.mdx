import { useEffect } from 'react';
import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { PaneProvider, Pane } from '@zendeskgarden/react-grid';
import { useArgs } from '@storybook/client-api';
import { PaneProviderStory, makeArray } from './stories/PaneProviderStory';
import { PANES } from './stories/data';

<Meta
  title="Packages/Grid/PaneProvider"
  component={PaneProvider}
  subcomponents={{ Pane, 'Pane.Content': Pane.Content, 'Pane.Splitter': Pane.Splitter }}
  args={{
    columnValues: {
      'column-a': 1,
      'column-b': 1
    },
    rowValues: {
      'row-1': 1,
      'row-2': 1
    },
    defaultColumnValues: {
      'column-a': 1,
      'column-b': 1
    },
    defaultRowValues: {
      'row-1': 1,
      'row-2': 1
    },
    panes: PANES,
    totalPanesWidth: 1000,
    totalPanesHeight: 1000
  }}
  argTypes={{
    panes: { name: 'panes', table: { category: 'Story' } }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story
    name="Uncontrolled"
    args={{ columnValues: undefined, rowValues: undefined }}
    argTypes={{ columnValues: { control: false }, rowValues: { control: false } }}
  >
    {args => <PaneProviderStory {...args} />}
  </Story>
</Canvas>

## Controlled

<Canvas>
  <Story
    name="Controlled"
    args={{ defaultColumnValues: undefined, defaultRowValues: undefined }}
    argTypes={{ defaultColumnValues: { control: false }, defaultRowValues: { control: false } }}
  >
    {args => {
      const updateArgs = useArgs()[1];
      const handleValueChange = (rowValues, columnValues) =>
        updateArgs({ rowValues, columnValues });
      return <PaneProviderStory {...args} handleValueChange={handleValueChange} />;
    }}
  </Story>
</Canvas>
