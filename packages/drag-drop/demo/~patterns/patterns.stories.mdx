import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { SortableListsStory } from './stories/SortableListsStory';
import { COLUMNS as columns } from './stories/data';

<Meta title="Packages/Drag-Drop/[patterns]" />

# Patterns

## Sortable Lists

Use `DraggableList`, `Dropzone`, and `Draggable` to create sortable lists.

<Canvas>
  <Story
    name="Sortable Lists"
    args={{ columns }}
    argTypes={{
      columns: {
        control: { type: 'object' },
        table: { category: 'Story' }
      }
    }}
  >
    {args => <SortableListsStory {...args} />}
  </Story>
</Canvas>
