import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { MultiDirectionDnDStory } from './stories/MultiDirectionDnDStory';
import { SingleDirectionDnDStory } from './stories/SingleDirectionDnDStory';
import { COLUMNS as columns } from './stories/data';

<Meta title="Packages/Drag-Drop/[patterns]" />

# Patterns

## Single Direction Drag and Drop

Use a combination of props to create a drag and drop experience.

<Canvas>
  <Story
    name="Single direction drag and drop"
    args={{
      columns,
      hasDropIndicator: true,
      hasPlaceholder: true,
      hasDanger: false,
      isCompact: false
    }}
    argTypes={{
      columns: { table: { category: 'Story' } },
      hasDropIndicator: { table: { category: 'Story' } },
      hasPlaceholder: { table: { category: 'Story' } },
      hasDanger: { table: { category: 'Story' } },
      isCompact: { table: { category: 'Story' } }
    }}
  >
    {args => <SingleDirectionDnDStory {...args} />}
  </Story>
</Canvas>

## Multi-Direction Drag and Drop

Use `DraggableList`, `Dropzone`, and `Draggable` to create a drag and drop experience.

<Canvas>
  <Story
    name="Multi-direction drag and drop"
    args={{ columns }}
    argTypes={{
      columns: {
        table: { category: 'Story' }
      }
    }}
  >
    {args => <MultiDirectionDnDStory {...args} />}
  </Story>
</Canvas>
