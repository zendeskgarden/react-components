import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { Button } from '@zendeskgarden/react-buttons';
import { ButtonStory } from './stories/ButtonStory';

<Meta
  title="Packages/Buttons/Button"
  component={Button}
  subcomponents={{ 'Button.StartIcon': Button.StartIcon, 'Button.EndIcon': Button.EndIcon }}
/>

# API

<ArgsTable />

# Demo

<Canvas>
  <Story
    name="Button"
    args={{ children: 'Text', hasStartIcon: false, hasEndIcon: false }}
    argTypes={{
      disabled: { control: 'boolean' },
      isEndIconRotated: {
        name: 'isRotated',
        control: 'boolean',
        table: { category: 'Button.EndIcon' }
      },
      isStartIconRotated: {
        name: 'isRotated',
        control: 'boolean',
        table: { category: 'Button.StartIcon' }
      },
      hasStartIcon: { name: 'Button.StartIcon', table: { category: 'Story' } },
      hasEndIcon: { name: 'Button.EndIcon', table: { category: 'Story' } }
    }}
  >
    {args => <ButtonStory {...args} />}
  </Story>
</Canvas>
