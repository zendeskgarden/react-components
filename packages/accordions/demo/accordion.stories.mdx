import { useState } from 'react';
import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { Accordion } from '@zendeskgarden/react-accordions';
import { AccordionStory } from './stories/AccordionStory';
import { ACCORDION_SECTIONS as SECTIONS } from './stories/data';

<Meta
  title="Packages/Accordions/Accordion"
  component={Accordion}
  subcomponents={{
    'Accordion.Header': Accordion.Header,
    'Accordion.Label': Accordion.Label,
    'Accordion.Panel': Accordion.Panel,
    'Accordion.Section': Accordion.Section
  }}
  args={{ isAnimated: true, level: 1, hasIconButtons: false, sections: SECTIONS }}
  argTypes={{
    defaultExpandedSections: { control: false },
    expandedSections: { control: false },
    hasIconButtons: { name: 'Accordion.Header icon buttons', table: { category: 'Story' } },
    sections: { name: 'Accordion.Section[]', table: { category: 'Story' } }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story name="Uncontrolled" args={{ isCollapsible: true }}>
    {args => <AccordionStory {...args} />}
  </Story>
</Canvas>

## Controlled

<Canvas>
  <Story
    name="Controlled"
    args={{ expandedSections: [0] }}
    argTypes={{ isCollapsible: { control: false }, isExpandable: { control: false } }}
  >
    {args => {
      const [expandedSections, setExpandedSections] = useState(args.expandedSections);
      const handleChange = index => {
        setExpandedSections(sections =>
          expandedSections.includes(index)
            ? sections.filter(section => section !== index)
            : [...expandedSections, index]
        );
      };
      return (
        <AccordionStory {...args} expandedSections={expandedSections} onChange={handleChange} />
      );
    }}
  </Story>
</Canvas>
