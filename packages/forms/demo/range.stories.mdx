import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { useArgs } from '@storybook/client-api';
import { Range, Field, Label, Hint, Message } from '@zendeskgarden/react-forms';
import { RangeStory } from './stories/RangeStory';

<Meta
  title="Packages/Forms/Range"
  component={Range}
  subcomponents={{ Field, Label, Hint, Message }}
  args={{
    label: 'Label',
    isLabelRegular: false,
    isLabelHidden: false,
    hasHint: true,
    hint: 'Hint',
    hasMessage: true,
    message: 'Message'
  }}
  argTypes={{
    disabled: { control: 'boolean' },
    min: { control: 'number' },
    max: { control: 'number' },
    step: { control: 'number' },
    label: { name: 'children', table: { category: 'Label' } },
    isLabelRegular: { name: 'isRegular', table: { category: 'Label' } },
    isLabelHidden: { name: 'hidden', table: { category: 'Label' } },
    hint: { name: 'children', table: { category: 'Hint' } },
    message: { name: 'children', table: { category: 'Message' } },
    validation: {
      control: { type: 'radio', options: ['success', 'warning', 'error'] },
      table: { category: 'Message' }
    },
    hasHint: { name: 'Hint', table: { category: 'Story' } },
    hasMessage: { name: 'Message', table: { category: 'Story' } }
  }}
  parameters={{
    design: {
      allowFullscreen: true,
      type: 'figma',
      url: 'https://www.figma.com/file/6g87L4FdKZTA3knt3Rsfdx/Garden?node-id=103%3A20263'
    }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story name="Uncontrolled">{args => <RangeStory {...args} />}</Story>
</Canvas>

## Controlled

<Canvas>
  <Story name="Controlled" args={{ value: '0' }}>
    {args => {
      const updateArgs = useArgs()[1];
      const handleChange = event => updateArgs({ value: event.target.value });
      return <RangeStory {...args} onChange={handleChange} />;
    }}
  </Story>
</Canvas>
