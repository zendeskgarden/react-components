import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { useArgs } from '@storybook/client-api';
import { Radio, Field, Label, Hint, Message } from '@zendeskgarden/react-forms';
import { RadioStory } from './stories/RadioStory';
import { commonArgs, commonArgTypes } from './stories/common';

<Meta
  title="Packages/Forms/Radio"
  component={Radio}
  subcomponents={{ Field, Label, Hint, Message }}
  args={{
    ...commonArgs,
    hasHint: false
  }}
  argTypes={{
    disabled: { control: 'boolean' },
    ...commonArgTypes
  }}
  parameters={{
    design: {
      allowFullscreen: true,
      type: 'figma',
      url: 'https://www.figma.com/file/6g87L4FdKZTA3knt3Rsfdx/Garden?node-id=103%3A20262'
    }
  }}
/>

# API

<ArgsTable />

# Demo

## Uncontrolled

<Canvas>
  <Story name="Uncontrolled">{args => <RadioStory {...args} />}</Story>
</Canvas>

## Controlled

<Canvas>
  <Story name="Controlled" args={{ checked: false }}>
    {args => {
      const updateArgs = useArgs()[1];
      const handleChange = event => updateArgs({ checked: true });
      return <RadioStory {...args} onChange={handleChange} />;
    }}
  </Story>
</Canvas>
