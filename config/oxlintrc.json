{
  "$schema": "../node_modules/oxlint/configuration_schema.json",
  "ignorePatterns": [".packages/**"],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "name": "react",
        "importNames": ["default"],
        "message": "Use named imports instead of the default React import."
      }
    ]
  }
}
